#!/usr/local/bin/cbsd
CBSD_SUDO=1
ARGS=$( . ${cbsdinit} )
if [ "$( id -u )" != "0" ]; then
	SUDO_CMD="/usr/local/bin/sudo -E"
else
	SUDO_CMD=
fi
exec /bin/sh <<EOF
${SUDO_CMD} /usr/local/cbsd/sudoexec/bcreate ${ARGS}
EOF
