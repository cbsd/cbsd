#!/bin/sh

if [ -z "${1}" ]; then
echo "$0 cmd"
exit
fi

workdir=${workdir:-"/usr/local/cbsd"}

case "${1}" in
"initenv")
    /bin/sh ${workdir}/tools/initenv
    exit
    ;;
*)
    /usr/bin/env PATH=${workdir}/tools:${workdir}/jailctl:${workdir}/nodectl:${workdir}/system:$PATH $@
    ;;
esac
