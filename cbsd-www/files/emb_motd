FreeBSD ?.?.?  (UNKNOWN)

*
** ZFSguru Embedded distribution
**
** You are now running a partially volatile distribution.
** This means that *some* changes you make will survive a reboot.
**
** Web-server with ZFS web-interface should be running
** Type ifconfig to discover your IP address (look for "inet x.x.x.x")
** Then enter http://<ip-address> in your webbrowser
*

Tip of the day:

