# sqllist "this|is|  it" a b c
# echo $a
sqllist()
{
    local _i _str IFS _key _T

    _str="$1"
    IFS="|"
    _i=2

    for _key in ${_str}; do
        eval _T=\$$_i
        _i=$((_i + 1))
        export ${_T}=${_key}
    done
}

