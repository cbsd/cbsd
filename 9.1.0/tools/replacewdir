#!/bin/sh
#v9.0.0
globalconf="${workdir}/cbsd.conf";
MYDESC="Replace for workdir"
MYARG="old"
MYOPTARG=""

[ -f ${globalconf} ] || err 1 "no such conf file";
. ${globalconf}
. ${subr}
init $*

shift
while [ -n "$1" ]; do
sed -i '' s:${old}:${workdir}:g $1
    shift
done

