#!/bin/sh
#v12.1.3
# Update jails/bhyve for ci_gw4
[ -z "${CIX_DISTDIR}" ] && CIX_DISTDIR="/usr/local/cbsd"
[ ! -r "${CIX_DISTDIR}/subr/cbsdbootstrap.subr" ] && exit 1
. ${CIX_DISTDIR}/subr/cbsdbootstrap.subr || exit 1
test_sql_stuff

[ ! -h "${dbdir}/local.sqlite" ] && exit
mydb="${dbdir}/local.sqlite"
unset _test _count
_test=$( ${miscdir}/sqlcli ${mydb} "SELECT COUNT(*) FROM pragma_table_info('jails') WHERE name = 'ci_gw4';" )
[ "${_test}" = "1" ] && exit 0
${ECHO} "  * ${N1_COLOR}Update jails tables: add ci_gw4${N0_COLOR}"
${miscdir}/sqlcli ${mydb} "ALTER TABLE jails ADD COLUMN ci_gw4 text default '0'"
