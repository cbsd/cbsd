if [ ! "$_CBSD_LINUX_SUBR" ]; then
_CBSD_LINUX_SUBR=1
###

ECHO="echo"
MORE_CMD="/bin/more"
TOUCH_CMD="/bin/touch"
GREP_CMD="/bin/grep"
SYSRC_CMD="/usr/local/cbsd/misc/sysrc"

# idprio hack
IDLE_IONICE="ionice -c 3"

# Linux require sed -i'', not sed -i ' '
SED_DELIMER=

###
fi
