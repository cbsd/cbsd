if [ ! "$_CBSD_LINUX_SUBR" ]; then
_CBSD_LINUX_SUBR=1
###

ECHO="echo"
MORE_CMD="/bin/more"
TOUCH_CMD="/bin/touch"
GREP_CMD="/bin/grep"

# idprio hack
IDLE_IONICE="${IONICE_CMD} -c 3"

# Linux require sed -i'', not sed -i ' '
SED_DELIMER=

###
fi
