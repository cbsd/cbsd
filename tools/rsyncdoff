#!/usr/bin/env cbsd
#v9.0.0
CIXARG=""
CIXOPTARG=""
MYDESC="Disable RSYNC service for jail migration"

. ${subrdir}/nc.subr

cixinit

. ${initenv}

rsyncd_disable
/usr/sbin/service rsyncd onestop

exit 0
