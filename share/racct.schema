# Default SQL scheme for DB racct
#MYTABLE="racct"
MYCOL="idx memoryuse maxproc openfiles pcpu readbps writebps readiops writeiops pmem"

idx="INTEGER PRIMARY KEY UNIQUE"
memoryuse="INTEGER DEFAULT 0"
maxproc="INTEGER DEFAULT 0"
openfiles="INTEGER DEFAULT 0"
pcpu="INTEGER DEFAULT 0"
readbps="INTEGER DEFAULT 0"
writebps="INTEGER DEFAULT 0"
readiops="INTEGER DEFAULT 0"
writeiops="INTEGER DEFAULT 0"
pmem="INTEGER DEFAULT 0"

INITDB=""
CONSTRAINT=""
