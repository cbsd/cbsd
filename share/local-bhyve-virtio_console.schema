# Default SQL scheme for DB local::virtio_console
. ${CIX_DISTDIR}/share/bhyve_virtio_console.conf

idx="INTEGER PRIMARY KEY AUTOINCREMENT"
con_name="TEXT NOT NULL UNIQUE"
con_socket="TEXT NOT NULL UNIQUE"

INITDB=""
CONSTRAINT=""
