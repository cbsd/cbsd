# Default SQL scheme for DB img::temp
#MYTABLE="forms"
MYCOL="idx name authkey"

idx="INTEGER PRIMARY KEY AUTOINCREMENT"
name="VARCHAR(64) UNIQUE DEFAULT ''"
authkey="TEXT DEFAULT NULL"

INITDB=""
CONSTRAINT=""
