# Default SQL scheme for DB img::temp
MYTABLE="projects"
MYCOL="id name servers_count jails_count modules_count size"

id='INTEGER  PRIMARY KEY AUTOINCREMENT NOT NULL'
name='varchar(255) NOT NULL'
servers_count="integer DEFAULT '0' NULL"
jails_count="integer DEFAULT '0' NULL"
modules_count="integer DEFAULT '0' NULL"
size="INTEGER DEFAULT '0' NULL"

INITDB=""
CONSTRAINT=""
