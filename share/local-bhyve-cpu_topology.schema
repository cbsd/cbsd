# Default SQL scheme for DB local::bhyve_cpu_topology
. ${sharedir}/bhyve_cpu_topology.conf

sockets="INTEGER DEFAULT 1"
cores="INTEGER DEFAULT 1"
threads="INTEGER DEFAULT 1"

INITDB=""
CONSTRAINT=""
