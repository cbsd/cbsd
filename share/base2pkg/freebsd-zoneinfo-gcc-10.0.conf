#

. ${sharedir}/base2pkg/freebsd-defaults-gcc-10.0.conf

_fakeroot="$fakeroot"

find_files="find ${_fakeroot}/usr/share/zoneinfo -type f"
find_dirs="find ${_fakeroot}/usr/share/zoneinfo -type d"
find_size="find ${_fakeroot}/usr/share/zoneinfo -type f -exec stat -f %z {} \+"

name="freebsd-zoneinfo-gcc"
origin="local/freebsd-zoneinfo gcc"
comment="FreeBSD zoneinfo gcc"
desc="|-
  FreeBSD zoneinfo gcc"
