. ${distsharedir}/vpc-peers.conf

# Default SQL scheme for DB local::vpc_peers
#MYTABLE="vpc_peers"

# MYCOL describe in vpc-peers.conf

id="INTEGER PRIMARY KEY AUTOINCREMENT"
src_node="TEXT"
src_ip="TEXT DEFAULT 0"
dst_node="TEXT"
dst_ip="TEXT DEFAULT 0"
peer_ip="TEXT UNIQUE"
vxlan_id="INTEGER"

CONSTRAINT=
INITDB=
