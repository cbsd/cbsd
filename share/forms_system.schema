# Default SQL scheme for DB img::system
#MYTABLE="system"
MYCOL="helpername helperdesc version packages have_restart longdesc"

helpername="INTEGER DEFAULT 0"
helperdesc="TEXT DEFAULT NULL"
version="INTEGER DEFAULT 0"
packages="INTEGER DEFAULT 0"
have_restart="INTEGER DEFAULT 0"
longdesc="TEXT DEFAULT NULL"

INITDB=""
CONSTRAINT=""
