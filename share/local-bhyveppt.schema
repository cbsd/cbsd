# Default SQL scheme for DB local::bhyveppt
#MYTABLE="bhyveppt"

MYCOL="ppt device vendor jname"

ppt="VARCHAR(64) UNIQUE DEFAULT ''"
device="TEXT DEFAULT NULL"
vendor="TEXT DEFAULT NULL"
jname="text default 0"

INITDB=""
CONSTRAINT=""
