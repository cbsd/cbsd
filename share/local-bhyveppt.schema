# Default SQL scheme for DB local::bhyveppt
#MYTABLE="bhyveppt"

MYCOL="ppt device vendor jname rom class subclass"

ppt="VARCHAR(64) UNIQUE DEFAULT ''"
device="TEXT DEFAULT NULL"
vendor="TEXT DEFAULT NULL"
jname="TEXT DEFAULT 0"
rom="TEXT DEFAULT 0"
class="TEXT DEFAULT 0"
subclass="TEXT DEFAULT 0"

INITDB=""
CONSTRAINT=""
