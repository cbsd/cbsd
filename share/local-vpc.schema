. ${distsharedir}/vpc.conf

# Default SQL scheme for DB local::vpc
#MYTABLE="bhyve"

# MYCOL describe in vpc.conf

id="INTEGER PRIMARY KEY AUTOINCREMENT"
init_node="TEXT DEFAULT 0"
vpc_name="TEXT UNIQUE"
bhyve_max="INTEGER DEFAULT 0"
jail_max="INTEGER DEFAULT 0"
node_member="TEXT DEFAULT 0"
peer_network="TEXT DEFAULT 0"

CONSTRAINT=
INITDB=
