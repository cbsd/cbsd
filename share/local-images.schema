. ${distsharedir}/images.conf

# Default SQL scheme for DB local::images
#MYTABLE="bhyve"

# MYCOL describe in images.conf

id="INTEGER PRIMARY KEY AUTOINCREMENT"
name="TEXT UNIQUE"
path="TEXT UNIQUE"
sources="TEXT DEFAULT 0"
created="TEXT DEFAULT 0"
emulator="TEXT DEFAULT 0"
size="INTEGER DEFAULT '0' NULL"

CONSTRAINT=
INITDB=
