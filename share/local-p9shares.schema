# Default SQL scheme for DB local::p9shares
. ${distsharedir}/p9shares.conf

p9path="TEXT NOT NULL"
p9device="VARCHAR(64) UNIQUE NOT NULL"
ro="BOOLEAN DEFAULT 'false' NULL"
options="TEXT DEFAULT 0"

INITDB=""
CONSTRAINT=""
