#!/bin/sh
#
# PROVIDE: scons
# BEFORE: var
# REQUIRE: mountcritlocal zfs
# KEYWORD: nojail

MODE=`vidcontrol -i mode |tail -n1| awk '{printf "MODE_"$1}'`

for ttyv in /dev/ttyv*; do
    vidcontrol ${MODE} < ${ttyv} > ${ttyv} 2>&1
done
