# Global base-in-pkg config
# Checkout FreeBSD-base-in-pkg-${ver} for version-specific/all settings

# default repo URL:
#base_in_pkg_repo_url='https://pkg.convectix.com/${ABI}/latest'
base_in_pkg_repo_url='pkg+https://pkg.FreeBSD.org/${ABI}/base_latest'

# default active packages ( main 'FreeBSD-runtime' always const/static ):
base_in_pkg_list_active="\
FreeBSD-utilities \
FreeBSD-rc \
FreeBSD-ssh \
FreeBSD-vi \
FreeBSD-csh \
FreeBSD-clibs \
FreeBSD-fetch \
FreeBSD-libarchive \
FreeBSD-libbsm \
FreeBSD-libbz2 \
FreeBSD-liblzma \
FreeBSD-libucl \
FreeBSD-openssl \
FreeBSD-openssl-lib \
FreeBSD-zoneinfo \
FreeBSD-libexecinfo \
"

# Also popular packages (see full list in FreeBSD-base_in_pkg-XX.conf ):
# FreeBSD-syslogd
# FreeBSD-clang
# FreeBSD-ssh

# you may want to expand the default list via ~cbsd/etc/FreeBSD-base_in_pkg.conf file, e.g.:
# --
# base_in_pkg_list_active="${base_in_pkg_list_active} \
#FreeBSD-clang \
#"
# --
