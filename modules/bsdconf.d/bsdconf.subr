prepare_jail()
{
	local i
	mountbase -o "" -p "" -d "" -c "" -s ""
	[ -r "${mount_fstab}" ] && mountfstab jname="${jname}"
	${miscdir}/mountpoint ${path}/dev || ${MOUNT_CMD} -t devfs devfs ${path}/dev
}
